<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.kaf.qrcodescanner">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarcodeScanner" />
        <activity android:name=".Workshop" />
        <activity android:name=".Seminar" />
        <activity android:name=".Talkshow"/>
        <activity android:name=".Sertifikat"/>
        <activity android:name=".SeminarSertifikat"/>
        <activity android:name=".WorkshopSertifikat"/>
        <activity android:name=".TalkshowSertifikat"/>
    </application>

</manifest>